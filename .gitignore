.DS_Store
.idea/
node_modules/
package-lock.json

# Local Lampa source used for plugin testing (see skill: lampa-plugin-browser-testing).
# Vendored locally so `npm run start` is one directory away — NOT committed.
lampa-source/

# Artifacts from Playwright MCP test runs
.playwright-mcp/

# Personal Lampa dev-launcher — boots lampa-source + pre-loads localStorage session.
# Not shipped; each dev runs their own.
lampa-dev.sh
scripts/
dev-session.json
.chrome-profile/

# Claude Code local tooling (personal)
.claude/
CLAUDE.md
openspec/
