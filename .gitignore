.DS_Store
.idea/
node_modules/
package-lock.json

# Vendored third-party sources (Lampa itself, etc.) — fetched locally per dev,
# never committed. See scripts/lampa-dev.sh for how it's consumed.
vendor/

# storage/ is tracked (holds generated extensions.json). Local-only files
# inside it (dev-session.json) are ignored via storage/.gitignore.

# Personal dev tooling — build scripts, Chrome launcher, puppeteer deps.
# Not shipped; each dev runs their own.
scripts/
.chrome-profile/

# Artifacts from Playwright MCP test runs
.playwright-mcp/

# Claude Code local tooling (personal)
.claude/
CLAUDE.md
openspec/
