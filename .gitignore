.DS_Store
.idea/
node_modules/
package-lock.json

# Local Lampa source used for plugin testing (see skill: lampa-plugin-browser-testing).
# Vendored locally so `npm run start` is one directory away — NOT committed.
lampa-source/

# Artifacts from Playwright MCP test runs
.playwright-mcp/

# Personal dev tooling — lives in ../dev/ (companion private repo,
# github.com/Vahagn-99/lampa-dev) and is symlinked into this tree for
# in-place use. No trailing slashes so the patterns match BOTH a local
# directory AND a symlink to one.
lampa-dev.sh
scripts
dev-session.json
.chrome-profile/
.claude
CLAUDE.md
openspec
